spring:
  cloud:
    azure:
      servicebus:
        namespace: msjad-servicebus
        entity-name: msjad-email-queue
        entity-type: queue
        connection-string: ${serviceBusConnectionString}

azure:
  storage:
    connection-string: ${storageAccountConnectionString}
    container-name: msajdsa-blob
